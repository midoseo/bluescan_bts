/* ===== demoVisits.js — 시연용 방문 결과 시드 =====
   방문 결과는 영업활동관리(SAMPLE_ACTIVITY)의 실제 매칭 2건(경성중고 거절·서울여자중학교 수주완료)만
   seedVisitsFromActivity로 주입하고, 여기서는 추가 자동 시드를 넣지 않는다(방문결과를 2건으로 정리).
*/
export function buildDemoVisits() {
  return {};
}
